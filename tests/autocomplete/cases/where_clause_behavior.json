{
  "group": "WHERE_CLAUSE_BEHAVIOR",
  "cases": [
    {
      "case_id": "WHERE_COMMA_001",
      "title": "WHERE after expression + comma suggests columns + functions",
      "sql": "SELECT * FROM users WHERE id IN (1, |",
      "dialect": "generic",
      "current_table": null,
      "schema_variant": "small",
      "expected": {
        "mode": "CONTEXT",
        "context": "WHERE_CLAUSE",
        "prefix": null,
        "comment": "Comma in WHERE clause = next-item. Suggest scope columns + functions.",
        "suggestions_contains": [
          "users.id",
          "users.name",
          "COUNT"
        ]
      }
    },
    {
      "case_id": "WHERE_WHITESPACE_001",
      "title": "WHERE after expression + space suggests logical keywords",
      "sql": "SELECT * FROM users WHERE id = 1 |",
      "dialect": "generic",
      "current_table": null,
      "schema_variant": "small",
      "expected": {
        "mode": "CONTEXT",
        "context": "WHERE_CLAUSE",
        "prefix": null,
        "comment": "Whitespace after expression = selection complete. Logical operators + clause keywords.",
        "suggestions_contains": [
          "AND",
          "OR",
          "ORDER BY"
        ]
      }
    }
  ]
}
